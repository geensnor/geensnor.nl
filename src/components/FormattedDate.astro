---
interface Props {
  date: Date;
}

const { date } = Astro.props;
---

<time datetime={date.toISOString()}>
  {
    new Date(date).toLocaleDateString("nl-NL", {
      year: "numeric",
      month: "short",
      day: "numeric",
    })
  }
</time>
